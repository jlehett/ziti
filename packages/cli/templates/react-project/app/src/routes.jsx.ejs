import { Route, Routes } from 'react-router-dom';
import Home from '@pages/home/home';
import App from './app';

export default (
    <Routes>
        <Route
            element={<App/>}
            path='/'
        >
            {/* Home Page */}
            <Route
                element={<Home/>}
                index
            />
        </Route>
    </Routes>
);
