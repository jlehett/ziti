import { CapacitorConfig } from '@capacitor/cli';

const config = {
  appId: 'io.ionic.starter',
  appName: '<%= projectPublicName %>',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
